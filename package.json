{
  "name": "happyplace",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w packages/frontend",
    "build": "npm run build -w packages/shared && npm run build -w packages/frontend",
    "fetch-data": "npx tsx scripts/fetch-fr.ts && npx tsx scripts/fetch-gb.ts && npx tsx scripts/fetch-de.ts && npx tsx scripts/fetch-us.ts && npx tsx scripts/fetch-ca.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "xlsx": "^0.18.5"
  }
}
